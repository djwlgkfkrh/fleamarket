<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="org.flea.mappers.BoardMapper">
	<!-- Read 시작 -->
	<update id="updateViewCnt">
		update board set viewcnt = viewcnt +1 where boardkey
		=#{boardkey}
	</update>
	<select id="read" resultType="org.flea.domain.BoardVO">
		select salestate, title, group1,
		group2, regdate, viewcnt, text, boardkey , userkey
		from board where
		boardkey =#{boardkey}
	</select>

	<select id="listSearchCount" resultType="int">
	 <![CDATA[ select count(boardkey) 
		from board where boardkey > 0 ]]>
		<include refid="search"></include>
	</select>

	<select id="listSearch" resultType="BoardVO">
		<![CDATA[  
		  select * from board where boardkey > 0 
		]]>
		<include refid="search"></include>
  
			<![CDATA[    
  				order by boardkey desc limit #{pageStart}, #{perPageNum} ]]>
	</select>
	<sql id="search">
		<if test="searchType != null">
			<if test="searchType == 't'.toString()">
				and title like CONCAT('%', #{keyword}, '%')
			</if>
			<if test="searchType == 'c'.toString()">
				and content like CONCAT('%', #{keyword}, '%')
			</if>
			<if test="searchType == 'w'.toString()">
				and writer like CONCAT('%', #{keyword}, '%')
			</if>
			<if test="searchType == 'tc'.toString()">
				and ( title like CONCAT('%', #{keyword}, '%') OR content
				like
				CONCAT('%', #{keyword}, '%'))
			</if>
			<if test="searchType == 'cw'.toString()">
				and ( content like CONCAT('%', #{keyword}, '%') OR writer
				like
				CONCAT('%', #{keyword}, '%'))
			</if>
			<if test="searchType == 'tcw'.toString()">
				and ( title like CONCAT('%', #{keyword}, '%')
				OR
				content
				like CONCAT('%', #{keyword}, '%')
				OR
				writer like CONCAT('%',
				#{keyword}, '%'))
			</if>
		</if>
	</sql>
	<select id="after" resultType="int">
	<![CDATA[  
		select boardkey
		from board where boardkey > #{boardkey} order by boardkey asc limit 1]]>
	</select>
	<select id="before" resultType="int">
	<![CDATA[  
		select boardkey from board where boardkey < #{boardkey} order by boardkey desc limit 1]]>
	</select>

	<select id="find" resultType="org.flea.domain.UserVO">
		select userkey, id, name, nickname,
		point, cheater from user where userkey=#{userkey}
	</select>
	
	<insert id="putcart">
		insert into cart (userkey, boardkey)
		values(#{userkey},#{boardkey})
	</insert>
	
	<select id="getcart" resultType="int">
		select count(*) from cart where
		boardkey=#{boardkey} and userkey=#{userkey}
	</select>

	<delete id="dealcart">
		delete from cart where boardkey=#{boardkey} and
		userkey=#{userkey}
	</delete>

</mapper>