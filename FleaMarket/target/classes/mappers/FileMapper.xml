<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="org.flea.mappers.FileMapper">

	<!-- boardkey = bno, fname = fullname -->


	<insert id="addFile">
		insert into file (boardkey, fname, oname, filetype, filesize, filepath, fileData)
		values (#{boardkey}, #{fname}, #{oname}, #{filetype}, #{filesize}, #{filepath}, #{fileData})
	</insert>

	<select id="getFile" resultType="org.flea.domain.FileVO">
		select filekey, fname, oname, filetype, filesize, filepath
		from file
		where boardkey=#{boardkey} <!-- 테스트 -->
	</select>


</mapper>