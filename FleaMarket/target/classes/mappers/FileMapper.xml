<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="org.flea.mappers.FileMapper">

	<!-- boardkey = bno, fname = fullname -->


	<insert id="addFile">
		insert into file (boardkey, fileid, fname, oname, filetype, filesize, filepath, fileData)
		values (#{boardkey}, #{fileid}, #{fname}, #{oname}, #{filetype}, #{filesize}, #{filepath}, #{fileData})
	</insert>

	<select id="getFile" resultType="org.flea.domain.FileVO">
		select boardkey, filekey, fileid, fname, oname, filetype, filesize, filepath, fileData
		from file
		where filekey=#{filekey} <!-- 테스트 -->
	</select>


</mapper>